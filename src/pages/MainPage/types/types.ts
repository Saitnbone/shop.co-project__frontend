import { ReactNode } from 'react';

export type TUiMainPage = {
  cover: ReactNode;
  slider: ReactNode;
  newArrivals: ReactNode;
  topSelling: ReactNode;
  dressStyle: ReactNode;
  happyCustomers: ReactNode;
};
